- [ ] 进入界面初始化
  - 获取当前的Model, 初始化界面上的Model选择框
  - 获取当前的Agent, 初始化界面上的Agent选择框
  - 根据Agent获取它的全部Sessions
  - 获取当前的SessionId, 从Sessions中选定的当前Session
  - 如果Session不为空，从Session中获取全部历史消息初始化对话界面
  - 根据SessionId获取当前ProjectDir，新增：要显示到左下角的当前角色状态的上方；
- [ ] 当打开右则的文件浏览器时，根据当前ProjectDir初始化文件浏览器；现在文件浏览器打开后一直是处于加载状态，完全不知道是否在加载；
- [ ] 获取当前Session的TokenUsage,显示到左下角的Token统计中；
- [ ] 如果当前没有Session，又没有ProjectDir, 那么首先要清理掉对话流中的信息，另外如果此时用户输入了内容点发送，那你就要先检查当前是否有SessionId, 如果没有就要先创建Session(自动触发 "新建立对话" 上的方法)，重新获取SessionId与ProjectDir, 然后才能发送消息；
- [ ] 如果无法获取当前Model，马上弹出供应商的选择框要用户选择供应商与Model;